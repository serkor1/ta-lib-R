% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart_theme.R
\name{chart_themes}
\alias{chart_themes}
\title{Chart Themes}
\description{
The charting system ships with a set of built-in color themes. Each
theme controls candle colors, background, text, grid lines, and a
10-color palette (colorway) used to distinguish indicator lines.

Use \code{\link[=set_theme]{set_theme()}} to apply or list themes.
}
\details{
\subsection{Available Themes}{

\describe{
\item{\code{default}}{A dark theme with cyan and steel-blue tones. Light
(\verb{#E0FFFF}) bullish candles on a near-black (\verb{#141414}) background
with a cool blue (\verb{#4682B4}) bearish candle. The colorway spans
icy blues through teal and soft purple.}
\item{\code{hawks_and_doves}}{A light, grayscale theme with a white background.
Candles use shades of gray, making it suitable for print or
presentations where color is secondary. The colorway uses muted,
accessible tones.}
\item{\code{payout}}{A dark teal theme on a near-black (\verb{#1A1A1A}) background.
Bullish candles are teal (\verb{#008080}), bearish candles are dark slate
(\verb{#2F4F4F}). The colorway follows the default Plotly palette.}
\item{\code{tp_slapped}}{A bright theme on a light gray (\verb{#ECF0F1})
background. Red (\verb{#E74C3C}) bearish and teal (\verb{#1ABC9C}) bullish
candles provide strong visual contrast. The colorway uses vivid,
saturated colors.}
\item{\code{trust_the_process}}{A subtle, earth-toned theme on a light gray
(\verb{#F5F5F5}) background. Both bull and bear candles use shades of gray,
keeping the focus on indicator lines. The colorway uses muted natural
tones.}
\item{\code{bloomberg_terminal}}{A dark theme on a near-black (\verb{#0E1017})
background with orange (\verb{#FF8F40}) bullish and neutral-gray
(\verb{#BBB9B2}) bearish candles. Inspired by the Bloomberg Terminal
interface. Colorblind-friendly (see below).}
\item{\code{limit_up}}{A dark monochrome theme on a near-black (\verb{#121212})
background. Candles use only luminance to encode direction (light-gray
bullish vs dark-gray bearish). Colorblind-friendly (see below).}
\item{\code{bid_n_ask}}{A light theme on an azure (\verb{#F0FFFF}) background
with steel-blue (\verb{#4682B4}) bullish and tomato-red (\verb{#FF6347})
bearish candles. The classic blue-vs-red trading pair.
Colorblind-friendly (see below).}
}
}

\subsection{Colorblind-Friendly Themes}{

The following themes encode bull/bear direction in ways that remain
distinguishable under the most common color-vision deficiencies. The
colorways for \code{bloomberg_terminal}, \code{limit_up}, and \code{bid_n_ask} are
derived from the Okabe & Ito (2008) qualitative palette, the de-facto
standard for accessible scientific visualization.

\describe{
\item{\code{limit_up}, \code{hawks_and_doves}, \code{trust_the_process}}{Encode
direction with luminance only. Safe under deuteranopia, protanopia,
tritanopia, and full achromatopsia.}
\item{\code{bloomberg_terminal}}{Orange + neutral gray. Safe under all
three CVD types thanks to Okabe-Ito-style hue separation.}
\item{\code{default}, \code{payout}}{Cyan/teal + blue or dark slate. Safe under
all three CVD types — the color pair sits inside the blue-yellow
axis that CVD users perceive normally.}
\item{\code{bid_n_ask}}{Blue + red. Safe under deuteranopia and
protanopia (the most common forms, affecting ~8\% of males); the
pair separates more weakly under tritanopia.}
}

\code{tp_slapped} is the only built-in that uses a teal/red pair adjacent to
the red-green CVD axis; prefer the themes above when accessibility
matters.
}

\subsection{Theme Properties}{

Each theme sets the following color properties:

\describe{
\item{Candle colors}{\code{bearish_body}, \code{bearish_wick}, \code{bearish_border},
\code{bullish_body}, \code{bullish_wick}, \code{bullish_border}}
\item{General colors}{\code{background_color}, \code{foreground_color} (axes and
borders), \code{text_color}}
\item{Grid and reference}{\code{gridcolor}, \code{threshold_color} (horizontal
reference lines such as overbought/oversold levels)}
\item{Colorway}{A vector of 10 colors cycled through for indicator
lines and legends}
}

Any of these properties can be individually overridden via the \code{...}
argument to \code{\link[=set_theme]{set_theme()}}.
}
}
\examples{
## list available themes
talib::set_theme()

## apply a theme by name
talib::set_theme("payout")

## apply a theme with custom overrides
talib::set_theme(
  "hawks_and_doves",
  background_color = "#FAFAFA"
)

## override individual properties
## without switching theme
talib::set_theme(
  bearish_body = "#FF4444",
  bullish_body = "#44FF44"
)

## reset to default theme
talib::set_theme("default")
}
\references{
Okabe, M. & Ito, K. (2008). \emph{Color Universal Design (CUD): How to make
figures and presentations that are friendly to colorblind people.}
\url{https://jfly.uni-koeln.de/color/}
}
\seealso{
Other Charting:
\code{\link[=chart]{chart()}},
\code{\link[=indicator]{indicator()}},
\code{\link[=set_theme]{set_theme()}}
}
\concept{Charting}
