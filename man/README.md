# Documentation

All .Rd files are untracked, and gets rendered but not committed during the Github Actions workflows.

If you have cloned the repo run:

```sh
make build
```

To render the .Rd files.