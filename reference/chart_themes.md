# Chart themes

Set the active chart color theme used by the package's chart rendering
functions.

## Value

Invisibly returns `.chart_variables` after modification.

## Details

Themes **mutate** the package-level theme state stored in
`.chart_variables`.

## See also

Other Chart Themes:
[`set_theme()`](https://serkor1.github.io/ta-lib-R/reference/set_theme.md)
